with open("app/src/main/kotlin/ee/ria/DigiDoc/ui/component/myeid/mydata/BiometricVerificationScreen.kt", "r") as f:
    content = f.read()

# I see the previous script failed to match again due to formatting, and also the "Overlay instruction" text isn't what I thought it was.
# Let me use a more robust regex or replace_with_git_merge_diff for safety.
